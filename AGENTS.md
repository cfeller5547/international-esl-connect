# ESL International Connect Agent Notes

Start here:
1. `docs/specs/15-agent-handoff-guide.md`
2. `docs/specs/README.md`

Source of truth:
- `docs/specs/*`

Current non-negotiables:
- Primary pillars are `Home`, `Learn`, `Speak`, `Tools`, `Progress`
- `Learn` is curriculum-only
- `Tools` contains Homework Help and Test Prep Sprint
- `User.currentLevel` is canonical
- qualifying assessments may promote level
- `mini_mock` never changes level
- curriculum MVP is English-only

Before changing behavior:
- read the relevant specs
- confirm current implementation in code
- update `docs/specs` in the same task if contracts change

When handing off:
- follow the template in `docs/specs/15-agent-handoff-guide.md`
